# pingcode-client

This project was generated by go-project-gen.

## Structure

- main.go: Application entry point
- cmd/: Command-line interface (Cobra)
- internal/app: Application logic
- internal/pkg: Library code
- info: Build information
- Makefile: Build scripts
- VERSION: Version file

## Usage

Run the project:

`+"```bash"+`
make run
`+"```"+`

Build the project:

`+"```bash"+`
make build
`+"```"+`
